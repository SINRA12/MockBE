const mongoose = require('mongoose');
const Schema = mongoose.Schema;

const dynamicRequestCallbackQparamsSchema = new Schema({
    key: String,
    value: String, 
    method: String
});

module.exports = mongoose.model("DynamicRequestCallbackQparams", dynamicRequestCallbackQparamsSchema);
